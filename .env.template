# LLM backend selection (ollama, openai, etc.)
LLM_BACKEND=ollama

# Model selection (qwen2.5:7b, qwen2.5:14b)
LLM_MODEL=qwen2.5:7b

# Controls randomness (lower = more deterministic)
LLM_TEMPERATURE=0.1
