name: datathon-env
channels:
  - conda-forge
dependencies:
  - python>=3.11
  # Environment management
  - python-dotenv
  # Core scientific libraries
  - numpy
  - scipy
  - matplotlib
  # Data handling
  - netCDF4
  - xarray
  - xcdat>=0.11.1
  # Parallel and image processing
  - dask
  - scikit-image
  # Agent / orchestration
  - langgraph
  # LLM clients
  - ollama
  - openai
  # Dev / QA
  - ruff
  - jupyterlab
