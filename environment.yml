name: datathon-env
channels:
  - conda-forge
dependencies:
  - python>=3.11
  - numpy
  - scipy
  - xarray
  - netCDF4
  - dask
  - zarr
  - matplotlib
  - scikit-image
  - opencv
  - pip
  - pip:
      - langgraph
      - llama-index
      # The following are open-source LLMs, typically run outside conda, but included for completeness
      # - llama3 # (install via pip or external, comment as placeholder)
      # - qwen2.5 # (install via pip or external, comment as placeholder)
      # - mixtral # (install via pip or external, comment as placeholder)
      - clip-by-openai # (optional, for vision-language alignment)
